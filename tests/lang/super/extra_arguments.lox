class Base {
  foo(a, b) {
    print "Base.foo(" + a + ", " + b + ")";
  }
}

class Derived < Base {
  foo() {
    print "Derived.foo()"; // out: Derived.foo()
    super.foo("a", "b", "c", "d"); // out: Expected 2 arguments but got 4.
  }
}

Derived().foo();
